import type { PlotScene } from '../../../types/plot';
import { encounterPlots } from '../shared/encounters';

export const day09Plots: Record<string, PlotScene> = {
  'daily_d09_wall_tap': encounterPlots['daily_d09_wall_tap'],
};
